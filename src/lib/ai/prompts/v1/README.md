# Prompts v1

Versioned prompts for the audit AI pipeline. Handlers import from here — never inline prompts in routes.

- `fix-issue.ts` — correction actionnable pour un `AuditIssue` (`generateObject` + schéma Zod).
